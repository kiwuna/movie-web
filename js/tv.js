async function catalogPage(){const grid=document.querySelector('#catalog-grid');loading(grid,12);const search=document.querySelector('#catalog-search'),genre=document.querySelector('#genre-filter'),year=document.querySelector('#year-filter'),sort=document.querySelector('#sort-filter');try{const data=await getTVGenres();data.genres.forEach(g=>genre.add(new Option(g.name,g.id)));genre.value=new URLSearchParams(location.search).get('genre')||'';function render(){loading(grid,12);const request=search.value.trim()?searchTV(search.value):discoverTV({sort_by:sort.value==='rating'?'vote_average.desc':sort.value==='newest'?'first_air_date.desc':'popularity.desc',with_genres:genre.value,first_air_date_year:year.value});request.then(d=>{grid.innerHTML=d.results.map(x=>card(x,'tv')).join('');document.querySelector('#result-count').textContent=`${d.results.length} shows found`;document.querySelector('#empty-state').hidden=!!d.results.length}).catch(e=>showError(grid,e.message))}[search,genre,year,sort].forEach(el=>el.addEventListener('change',render));search.addEventListener('input',()=>{clearTimeout(search.timer);search.timer=setTimeout(render,350)});document.querySelector('#clear-filters').onclick=()=>{search.value=genre.value=year.value='';render()};render()}catch(e){showError(grid,e.message)}}catalogPage();
