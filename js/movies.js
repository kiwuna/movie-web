async function loadHome(){const hero=document.querySelector('#hero');if(!hero)return;['trending','popular-movies','popular-tv','now-playing','upcoming'].forEach(id=>loading(document.querySelector(`#${id}`)));try{const [trending,popular,tv,now,upcoming]=await Promise.all([getTrendingMovies(),getPopularMovies(),getPopularTV(),getNowPlayingMovies(),getUpcomingMovies()]);const featured=trending.results[0];hero.style.backgroundImage=`url('${imageUrl(featured.backdrop_path,'original')}')`;hero.innerHTML=`<div class="hero-content"><p class="eyebrow">Featured premiere</p><h1>${titleOf(featured)}</h1><div class="meta"><span>${yearOf(featured)}</span><span class="rating">★ ${featured.vote_average.toFixed(1)}</span></div><p class="hero-description">${featured.overview||'Discover what everyone is talking about.'}</p><div class="hero-actions"><a class="button primary" href="details.html?id=${featured.id}&type=movie#preview">▶ Watch preview</a><a class="button secondary" href="details.html?id=${featured.id}&type=movie">More info</a></div></div>`;document.querySelector('#trending').innerHTML=trending.results.slice(0,10).map(x=>card(x,'movie')).join('');document.querySelector('#popular-movies').innerHTML=popular.results.slice(0,10).map(x=>card(x,'movie')).join('');document.querySelector('#popular-tv').innerHTML=tv.results.slice(0,10).map(x=>card(x,'tv')).join('');document.querySelector('#now-playing').innerHTML=now.results.slice(0,10).map(x=>card(x,'movie')).join('');document.querySelector('#upcoming').innerHTML=upcoming.results.slice(0,10).map(x=>card(x,'movie')).join('');const genres=await getMovieGenres();document.querySelector('#genres').innerHTML=genres.genres.map(g=>`<a class="genre" href="movies.html?genre=${g.id}">${g.name}</a>`).join('')}catch(error){showError(hero,error.message)}}async function catalogPage(type){const grid=document.querySelector('#catalog-grid');if(!grid)return;loading(grid,12);const search=document.querySelector('#catalog-search'),genre=document.querySelector('#genre-filter'),year=document.querySelector('#year-filter'),sort=document.querySelector('#sort-filter'),genreId=new URLSearchParams(location.search).get('genre')||'';try{const genreData=await(type==='movie'?getMovieGenres():getTVGenres());genreData.genres.forEach(g=>genre.add(new Option(g.name,g.id)));genre.value=genreId;function render(){loading(grid,12);const params={sort_by:sort.value==='rating'?'vote_average.desc':sort.value==='newest'?(type==='movie'?'primary_release_date.desc':'first_air_date.desc'):'popularity.desc',with_genres:genre.value,year:year.value};const request=search.value.trim()?(type==='movie'?searchMovies(search.value):searchTV(search.value)):(type==='movie'?discoverMovies(params):discoverTV(params));request.then(data=>{grid.innerHTML=data.results.map(x=>card(x,type)).join('');document.querySelector('#result-count').textContent=`${data.results.length} ${type==='movie'?'movies':'shows'} found`;document.querySelector('#empty-state').hidden=!!data.results.length}).catch(e=>showError(grid,e.message))}[search,genre,year,sort].forEach(el=>el.addEventListener('change',render));search.addEventListener('input',()=>{clearTimeout(search.timer);search.timer=setTimeout(render,350)});document.querySelector('#clear-filters').onclick=()=>{search.value=genre.value=year.value='';render()};render()}catch(e){showError(grid,e.message)}}loadHome();catalogPage('movie');
